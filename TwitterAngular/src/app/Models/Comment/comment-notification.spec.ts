import { CommentNotification } from './comment-notification';

describe('CommentNotification', () => {
  it('should create an instance', () => {
    expect(new CommentNotification()).toBeTruthy();
  });
});
