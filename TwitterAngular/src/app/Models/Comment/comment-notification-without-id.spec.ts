import { CommentNotificationWithoutId } from './comment-notification-without-id';

describe('CommentNotificationWithoutId', () => {
  it('should create an instance', () => {
    expect(new CommentNotificationWithoutId()).toBeTruthy();
  });
});
